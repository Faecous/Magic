using UnityEngine;

/// <summary>
/// Manages display of health, stamina, etc.
/// </summary>
public class PlayerHUD : MonoBehaviour
{
    // TODO: Add references to UI elements (sliders, text, images).
    // Implement public methods to update the HUD based on events from other systems
    // (e.g., OnHealthChanged, OnStaminaChanged).
} 