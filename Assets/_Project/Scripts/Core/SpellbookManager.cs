using UnityEngine;
using System.Collections.Generic;

/// <summary>
/// Loads all SpellData into a central database at runtime.
/// </summary>
public class SpellbookManager : MonoBehaviour
{
    // TODO: Implement logic to find and load all SpellData ScriptableObjects from project assets.
    // public Dictionary<string, SpellData> SpellDatabase { get; private set; } = new Dictionary<string, SpellData>();
} 